// Cpp is faster than java because it's a compiled language and hence is compiled to binaries, so it runs immediately
//Java is an interpreted language so it must first be interpreted during run-time so it runs slower

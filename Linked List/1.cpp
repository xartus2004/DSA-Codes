//Problems with arrays

//1. Either the size is fixed or pre-allocated(in both fixed and variable sized arrays) or the worst case insersion at the end is O(n).
//2.Insertion and deletion in the middle(or beginning) is costly.
//3.Implementation of data structures like queue and deque is complex with array